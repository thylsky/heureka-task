import React from 'react';
import { StyledSpinner, Wrapper } from './Spinner.styles';

const Spinner = () => {
  return (
    <Wrapper>
      <StyledSpinner />
    </Wrapper>
  );
};

export default Spinner;
